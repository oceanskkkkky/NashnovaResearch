# 2026-08-23

## stock-almanac-daily 移动端改造 + 实跑

- 为 skill 新增 `format` 参数（desktop/mobile/both）：新增 `references/mobile-spec.md`（竖屏卡片流、可见预算≤1500单位、emoji≤15、小红书语感）、`assets/report-shell-mobile.html`（460px 单列骨架，保留墨色/朱砂/金令牌与全部必需栏目 id）；校验脚本加 `--format mobile` 检查（移动端签名、emoji 计数、禁止残留 1268px 桌面宽度），桌面校验规则不变且旧报告回归通过。
- 修正 data-routing.md：`westock lhb` 全市场榜只支持 `--date` 单日，不支持 `--start/--end`（个股 `fund lhb` 支持区间）。
- 以 format=mobile 实跑一期（评分日 2026-08-21，休市版）：市场环境分约46（中性偏弱），重点板块=贵金属(热度81)/工业金属(71)，唯一入选=紫金矿业(最终分73.4，条件式参与，观察区间33.0—34.2、失效位29.7)；湖南黄金停牌被硬过滤，盛达资源质押35.68%扣分出局。
- 产物：`reports/stock-almanac/2026-08-23-stock-almanac-mobile.html`（校验 PASS，可见1189单位≈3.7分钟）+ `2026-08-23-xiaohongshu-note.md`。
- NashNova 本期 fin news/websearch coverage=empty（退出码3）、fin research 向量化不可用（退出码4），按降级路径改用 WeStock hot news + report list。
- 小红书自动发布评估：SkillHub 与本地技能市场（292个）均无小红书发布技能，无官方 API；可行方案为半自动（生成产物→agent-browser 打开创作者平台预填→人工点发布），全自动有风控/封号风险。用户选择半自动流水线，已创建自动化 automation-1787490494859（周一至周五 17:30，收盘后生成+截图+预填发布页，不自动点发布）。

## skill 三项规则修改（用户要求）

1. 「执行摘要」改「行情摘要」：桌面/移动端骨架、report-spec、mobile-spec 同步更新，id 保持 `executive-summary` 不变。
2. 证据区在分析过程保留但不进产物：产物禁止 `<details id="evidence">` 与 `[E01]` 编号（校验脚本新增 BANNED_IDS + EVIDENCE_REF 检查），证据台账改存同目录 `YYYY-MM-DD-evidence.md` 旁车文件，关键数据缺口并入风险提示/方法折叠区。
3. 入选放宽：scoring.md 入榜门槛 常规≥72→≥65、中性市场优先≥75→≥68、防守<45 由80→70；数量上限 4→5只（max_picks 限制 1—5）。
- 按新规则重出 2026-08-23 移动端日报：入选3只——紫金矿业73.4（条件式参与）、中金黄金70.4（逢回调观察）、山东黄金69.0（逢回调观察，超买不追）；赤峰黄金66.0因同板块限2只未入榜。校验 PASS（1455单位≈4.5分钟）。配套更新小红书文案与 evidence 旁车文件。
- 注意：2026-08-22 旧桌面报告含证据区，在新校验规则下会 FAIL（历史产物，属预期）。

## 21:38 按用户要求重新生成日报（format=both）

- 复核交易日（仍为 2026-08-21）并刷新 hot news（新增钨业个股/ST公告等，与入选板块无关，不构成新催化），评分结论不变。
- 新产出桌面版 `2026-08-23-stock-almanac.html`（PASS，2091单位≈6.5分钟，emoji=0）；移动端 HTML 复校 PASS（1455单位）。同一份分析两产物，未重复取数；evidence 旁车文件追加复核记录。

## skill 迁移到项目级 + git 提交

- `stock-almanac-daily` 从用户级 `C:\Users\tizyt\.workbuddy\skills\` 迁移到项目级 `D:\UGit\nashnova\.workbuddy\skills\`（复制→diff校验一致→删除原目录），随仓库管理。
- git 本地提交 6d81d25（7文件：4份日报产物+3个记忆文件）；push 失败——github.com 直连被重置（schannel/openssl 均 TLS 中断，机器无代理），待网络恢复后 `git push origin main` 补推。

## 小红书自动发布尝试（22:00-23:45，未成功，转手动）

- 环境：playwright 已装入隔离 venv（清华镜像装包，npmmirror 下载 Chromium；注意 safe-delete/回收站不可用是 WorkBuddy Python shim 拦截 unlink，别在脚本里 unlink 文件，用 mtime 判别新信号）。
- **重要坑**：python+playwright 需 `dangerouslyDisableSandbox` 运行；`page.close()/browser.close()` 在本机触发原生崩溃（exit 1 无输出），脚本一律避免 close，用 `os._exit(0)` 收尾。
- 小红书创作者平台发布页真实结构：左上角红色"发布笔记"是**导航菜单**（切换视频/图文/长文），右下角红色"发布"才是提交按钮，且**该按钮要等图片处理+合规检查完成后才渲染**（约30s-4min+，自动会话被风控时可能永不出现）。
- 自动会话 4 次尝试均未真正发布：疑似被平台风控标记（上传/填充正常，但发布按钮不渲染）。转手动：9张图 + 标题 + 正文已在 `outputs/小红书手动发布包-2026-08-23.md`，图片在 `reports/stock-almanac/2026-08-23-xhs-images/`（封面 01-cover.png 为专用1080×1440设计稿）。
- 经验：小红书 web 自动发布不可靠（按钮延迟渲染+风控），半自动（填好截图+人工点）或纯手动更稳；长期建议接入官方 API/第三方合规发布通道。
