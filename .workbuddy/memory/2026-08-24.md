# 2026-08-24 工作日志

## 股市老黄历自动化（automation-1787490494859，部分完成）

- analysis_session=2026-08-21（周五收盘），周一盘前运行，不标休市版。
- 完成：nashnova update（0.2.2 最新）、westock trade-calendar 确认交易日、全量取数（大盘/宏观/板块/龙虎榜/资金/技术/财务/风险/一致预期/新闻）。
- 产出：`reports/stock-almanac/2026-08-24-evidence.md`（证据台账）。
- 结论：MarketEnvironment≈51 中性；入选板块仅贵金属（SectorHeat≈79）；条件式参与=中金黄金（71.8）、山金国际（69.6）；逢回调观察=赤峰黄金（65.9）、中际旭创（独立逻辑 62.0）。
- 降级：NashNova fin news/websearch 均 coverage=empty，新闻改用 WeStock hot news/news list。
- 未完成（被新指令打断）：移动端 HTML 生成与校验、竖版截图、小红书预填。

## git 提交与推送

- 提交 b9d7a5b（dev 分支）：小红书发布流程改手动确认、08-23 竖版素材、08-24 证据台账、gitignore 排除 xhs-profile（114MB 登录态，敏感）与调试/运行态文件。
- 推送受阻：HTTPS(443) TLS 握手被网络层切断（schannel/openssl 均失败，重试 20+ 次）；SSH 22 端口可达但未配密钥。
- 已生成 `~/.ssh/id_ed25519`（无口令），公钥待用户添加到 GitHub 后切 SSH 推送；仓库级配置已改 http.sslBackend=openssl + HTTP/1.1。

## 2026-08-24 股市老黄历日报（mobile + 小红书发布）

- 优化 stock-almanac-daily skill：新增 `references/xhs-preflight.md`（XHS 合规微调规则），更新 SKILL.md 工作流与 mobile-spec.md（个股点位形态描述化、免责声明可见化、标签中性化）。
- 生成 2026-08-24 mobile 日报：评分日 2026-08-24 已收盘，上证 -0.59% / 深成 -2.13% / 创业板 -3.21%，成交 20075 亿（20日均值 87%、较上日放量 1282 亿）。MarketEnvironment≈44 → 防守，门槛 70、最高 2 只；中金黄金 68.3、紫金矿业 65.3、招金黄金 57.5 全部未达 70 → **零推荐**，输出 2 只回调观察池（中金黄金、紫金矿业）。validate PASS：可见 1313 单位 / emoji 7。
- 8 张小红书卡片图已生成（reports/stock-almanac/2026-08-24-xhs-images/）。
- 本机 NashNova 未安装 + westock-premarket 缺 token → 新闻/研报降级用 WeStock + 公开报道 WebSearch 核实（仅定性事件），台账显式披露。
- 小红书发布：skill 的 `social-auto-upload`（Linux `/root/clawd`）不可用；用 `outputs/xhs-publish.js` 临时方案（Playwright 持久化 profile `.workbuddy/xhs-profile`）后台运行，已识别到登录跳转、缺多文件 input 等问题，迭代到支持 multiple input / 单文件逐张 fallback。等用户扫码登录后继续。

## 安装 hithink-finance skill（同花顺金融数据）

- 通过 SkillHub CLI 安装 `@org-6602ci5n/hithink-finance`（v1.0.5）到 `C:\Users\tizytian\.workbuddy\skills\hithink-finance\`。
- skillhub CLI 本机已存在（`~/.skillhub/skills_store_cli.py`，v2026.6.27），Git Bash 下 `command -v skillhub` 失效因 `~/.local/bin` 不在 PATH 且 wrapper 的 POSIX 路径会被 Windows python.exe 误解析；正确调用：`"C:/Users/tizytian/.workbuddy/binaries/python/versions/3.13.12/python.exe" "C:/Users/tizytian/.skillhub/skills_store_cli.py" <subcmd>`。
- 安装时 skillhub 默认生成 `@org-6602ci5n/hithink-finance` 两层嵌套，已扁平化到 `skills/hithink-finance/`（workbuddy 标准结构，删除空 namespace 目录）。
- 安全审计通过：纯文档型（SKILL.md + references/*.md + _meta.json + agents/openai.yaml），无任何可执行脚本，无 P0/P1 风险。
- 该 skill 是同花顺金融数据服务统一入口（A股行情/财报/估值/指数/板块/公募基金/DuckDB），支持 CLI/MCP/REST/Python SDK 四种接入。

## hithink-finance 接入与替换 NashNova 数据源

- 凭据配置：API Key 写入 `%APPDATA%\hithink-finance\credentials.env`（单行 `HITHINK_FINANCE_API_KEY=...`），不落命令参数/项目文件。
- CLI 安装：`npm i -g @hithink-tech/hithink-finance-cli`，位于 `C:\Users\tizytian\.workbuddy\binaries\node\versions\22.22.2-1\hithink-finance`；`auth login --api-key-stdin` 无感登录成功。
- 线上验证通过：`symbol search`、`market snapshot`、REST 三个端点（snapshot/dragon-tiger/limit-up-pool）、`special limit-up-pool`（含涨停原因+连板数）、`special hot-stock` 均返回 `ok:true` 真实数据。
- 数据源替换：stock-almanac-daily skill 全面去 NashNova 化，改动 SKILL.md + references/data-routing.md + references/xhs-preflight.md，并同步到 outputs 副本。新分工：hithink-finance 管行情/日线/指数/板块/财报/估值/特色数据（涨停池/连板/龙虎榜/异动/热股），WeStock 管交易日历/大盘/宏观/资金/公告/风险，新闻研报事实核查走 WeStock + WebSearch（只核实事件不补造数据）。
- 注意：`outputs/stock-almanac-daily` 副本整体滞后于 `.workbuddy/skills` 项目级版本（缺 mobile-spec.md、report-shell-mobile.html 等），本次仅同步了改动的 3 个文件；项目级副本为 WorkBuddy 实际加载版本。
- 能力边界：hithink-finance 无新闻/研报语义/宏观/资金流向接口。
